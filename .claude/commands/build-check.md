# 프로덕션 빌드 검증

프로덕션 빌드가 정상적으로 완료되는지 확인합니다.

## 실행

```bash
npm run build
```

## 목적

- 프로덕션 배포 전 빌드 검증
- 런타임 에러 사전 탐지
- 최적화된 번들 생성

## 사용 시점

- PR 생성 전
- 배포 전
- 주요 기능 추가 후

## 빌드 결과

성공 시:
- `.next/` 폴더에 빌드 결과 생성
- 번들 크기 정보 출력

실패 시:
- 에러 로그 확인
- 타입 에러, import 에러 등 수정
