# TypeScript 타입 체크

프로젝트 전체의 TypeScript 타입을 검증합니다.

## 실행

```bash
npx tsc --noEmit
```

## 목적

- 컴파일 에러 없이 타입이 안전한지 확인
- 빌드 전 타입 검증
- CI/CD 파이프라인에서 실행

## 사용 시점

- 코드 변경 후
- PR 생성 전
- 빌드 전

## 에러 해결

타입 에러 발생 시:
1. 에러 메시지에서 파일과 라인 번호 확인
2. 해당 파일 열어서 타입 수정
3. `any` 사용하지 말고 정확한 타입 지정
